pipeline {
    agent any
    stages {
        stage('Docker'){
            steps{
                sh 'docker build -t my-playwright .'
            }
        }
        stage('Build Docker Image') {
            agent {
                docker {
                    image 'node:18-alpine'
                    reuseNode true
                }
            }
        }
    }
}